'use strict';

(function (scope, logging) {
  var logger = logging.getLogger('renderer');

  scope.CanvasRender.prototype.drawMathPrimitive = function() {
    logging.debug("MathCanvasRender is not implemented yet");

  };

}(MyScript, logging))
